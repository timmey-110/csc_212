 using git in vs codes terminal was generally the same. i noticed that i do not need to use the terminal to clone a repo because vs code has an option to clone repo so i just select the repo i want to clone from the list of repos. through the terminal though, the terminologies are the same

