#include "genetic_gap.h"

std::uint32_t genetic_gap(const DNA &dna1, const DNA &dna2) {
    // TODO: Return the minimum number of mutations (insertion, deletion, replacement) to transform `dna1` into `dna2`.
}
