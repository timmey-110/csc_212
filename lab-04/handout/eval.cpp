#include "eval.h"

int eval(const std::string &expr) {
    // TODO: Evaluate `expr`. Assume `expr` is a valid fully parenthesized infix expression. All operands are integers.
    // All operators are either '+', '-', '*' and '/' where '/' is integer division in C++.
}
