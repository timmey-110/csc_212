#include "find_max_distance.h"

size_t find_max_distance(const std::vector<int> &v) {
    // TODO: Return the maximum distance between occurrences of the maximum value in `v`.
    //
    // Examples:
    // find_max_distance({}) == 0
    // find_max_distance({9}) == 0
    // find_max_distance({-1, -1}) == 1
    // find_max_distance({4, 2, 0, 4, 4, 2}) == 4
}
