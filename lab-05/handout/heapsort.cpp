#include "heapsort.h"

void heapsort(std::vector<int> &vec) {
    // TODO: Sort `vec` with heapsort. You may use `std::priority_queue`, the `std::*_heap` functions or your own
    // priority queue implementation.
}
